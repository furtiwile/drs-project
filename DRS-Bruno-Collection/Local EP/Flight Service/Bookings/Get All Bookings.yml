info:
  name: Get All Bookings
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_flights_url}}/airlines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
