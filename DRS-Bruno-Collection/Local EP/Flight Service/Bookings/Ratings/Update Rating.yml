info:
  name: Update Rating
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{local_flights_url}}/airports/7"
  body:
    type: json
    data: "{\r

      \    \"name\" : \"\"\r

      \    //Either or both are possible\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
