info:
  name: Update Airport
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{local_flights_url}}/airports/7"
  body:
    type: json
    data: "{\r

      \    \"name\" : \"\"\r

      \    //Either or both are possible\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
