info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{local_server_url}}/api/v1/auth/login"
  body:
    type: json
    data: "{\r

      \    \"email\" : \"admin@example.com\",\r

      \    \"password\" : \"password\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
