info:
  name: Get All Airports
  type: http
  seq: 1

http:
  method: GET
  url: "{{local_server_url}}/airports?page=3&per_page=4"
  params:
    - name: page
      value: "3"
      type: query
    - name: per_page
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
