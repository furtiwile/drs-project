info:
  name: Get Airport
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_server_url}}/airports/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
