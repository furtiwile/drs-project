info:
  name: Login Copy
  type: http
  seq: 5

http:
  method: POST
  url: "{{docker_server_url}}/api/v1/auth/login"
  body:
    type: json
    data: "{\r

      \    \"email\" : \"admin@example.com\",\r

      \    \"password\" : \"password\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
