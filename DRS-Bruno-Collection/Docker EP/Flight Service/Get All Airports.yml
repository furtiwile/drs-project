info:
  name: Get All Airports
  type: http
  seq: 1

http:
  method: GET
  url: "{{docker_flights_url}}/airports"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
