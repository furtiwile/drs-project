info:
  name: Get Airline
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_server_url}}/airlines/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
