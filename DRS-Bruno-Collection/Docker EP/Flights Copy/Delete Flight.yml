info:
  name: Delete Flight
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{local_server_url}}/flights/9"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
