info:
  name: Get Flight
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_server_url}}/flights/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
