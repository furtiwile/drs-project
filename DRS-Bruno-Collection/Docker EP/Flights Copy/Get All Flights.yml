info:
  name: Get All Flights
  type: http
  seq: 1

http:
  method: GET
  url: "{{local_server_url}}/flights"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
